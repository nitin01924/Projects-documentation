package com.syncsound.app.ui

import androidx.lifecycle.ViewModel

/**
 * Home currently holds no domain state; this ViewModel preserves the screen's
 * MVVM boundary as onboarding and remembered groups are added later.
 */
class HomeViewModel : ViewModel()
